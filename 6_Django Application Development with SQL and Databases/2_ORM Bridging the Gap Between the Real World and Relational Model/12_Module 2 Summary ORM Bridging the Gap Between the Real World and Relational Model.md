## 🔗 Bölüm 2 Özeti: ORM: Gerçek Dünya ile İlişkisel Model Arasındaki Köprü

Tebrikler! Bu dersi tamamladınız. Kursun bu aşamasında şunları biliyorsunuz:

* Nesne Yönelimli Programlama ( *Object-Oriented Programming* ,  *OOP* ) paradigması ile SQL paradigması veriyi farklı şekilde modeller.
* *Object Relational Mapping* ( *ORM* ), OOP ile SQL arasındaki farkı kapatır.
* ORM kütüphaneleri veya araçları, ilişkisel bir veritabanında satırlar olarak saklanan veriyi nesnelere ya da nesneleri satırlara eşleyip aktarabilir.
* ORM, nesneleri satırlara ve satırları nesnelere dönüştürdüğü için, geliştiricilerin veriyi sorgulamak ve veriyi değiştirmek için OOP kullanmasına imkân tanır.
* Django ORM, Django web uygulama çatısına ait bir Python ORM bileşenidir.
* Django ORM, her Django modeli için bir veritabanı tablosuna eşlemeler tanımladığınız için veritabanı geliştirmesini hızlandırmaya yardımcı olabilir.
* Her Django alanı ( *field* ), bir sütun türüne eşlenir.
* Modeller ve alanlar tanımlandıktan sonra Django tabloları otomatik olarak oluşturur.
* Django API’leri, veritabanı nesneleri üzerinde Create, Read, Update ve Delete ( *CRUD* ) işlemlerini gerçekleştirebilir.
* Bir Django modelinde, bir nesne oluşturur ve onu veritabanına bir kayıt olarak eklemek için modelin `save` metodunu çağırırsınız.
* Django Model API’yi kullanarak nesneleri okumak için, model sınıfınız üzerinde bir `Manager` kullanarak bir `QuerySet` oluşturmanız gerekir.
* Django’da nesneleri güncelleyerek veritabanı kayıtlarını güncellemenin birkaç yolu vardır.
* Bir veritabanındaki kayıtları silmek için, bir model nesnesi veya bir `QuerySet` üzerinde Django ORM’in `delete` metodunu çağırırsınız.
