# 🎓 Modül Özeti ve Öne Çıkanlar

Tebrikler! Bu modülü tamamladınız. Bu noktada, şunları biliyorsunuz:

---

## 🐚 Kabuk ve Dosya/Dizinlerle Çalışma

Bir kabuk ( *shell* ), etkileşimli bir kullanıcı arayüzüdür. Dosya ve dizinler arasında gezinmek ve onlarla çalışmak için kabuk komutlarını kullanırsınız.

---

## 🌐 URL’lerden Dosya Görüntüleme ve İndirme

`curl` ve `wget` komutları, URL’lerden dosyaları görüntüler ve indirir; `cat` ve `tail` komutları ise dosya içeriklerini görüntüler.

---

## 🧾 Sistem, Kullanıcı ve Tarih Bilgileri

Kullanıcı bilgilerini `whoami` ve `id` komutlarıyla alabilir, işletim sistemi bilgilerini `uname` komutuyla elde edebilirsiniz. Sistem disk kullanımını `df` komutunu kullanarak kontrol edebilir, süreçleri ve kaynak kullanımını `ps` ve `top` ile izleyebilirsiniz. `echo` ile bir dizgenin veya değişken değerinin çıktısını alabilir, `date` komutuyla tarih hakkında bilgi yazdırıp çıkarabilir ve `man` ile herhangi bir komutun kılavuzunu okuyabilirsiniz.

---

## 📂 Dizinlerde Gezinme ve Dosya Arama

`ls`, belirtilen bir dizin ağacındaki tüm dosya ve dizinleri listeler ve `cd` dizinler arasında gezinmenizi sağlar. `find` komutu, dizinlerinizdeki dosyaları bulur.

---

## 📁 Göreli ve Mutlak Yollar

Göreli yollar, geçerli çalışma dizininize göre belirlenirken; mutlak yollar bağımsız olarak, tek başlarına anlam taşır.

---

## 🏗️ Dosya ve Dizin Oluşturma, Silme, Taşıma

Dosya ve dizinleri `touch` ve `mkdir` komutlarıyla oluşturabilir, `rm` ve `rmdir` ile silebilir ve `cp` ile `mv` komutlarıyla kopyalayıp taşıyabilirsiniz.

---

## 📄 Dosya İçeriği Görüntüleme ve Sayımlar

`cat`, `more`, `head` ve `tail` komutları, dosya içeriklerini sıralı şekilde görüntülemenizi veya yalnızca belirli sayıda satır görmenizi sağlar. Satır, kelime ve karakter sayılarını `wc` ile belirleyebilirsiniz.

---

## 🔤 Metin Sıralama ve Filtreleme

`sort` komutunu, bir dosyanın satırlarını alfanümerik olarak görüntülemek için kullanabilir ve `uniq` ile tekrar eden satırları görünümünüzden kaldırabilirsiniz. `grep`, belirlediğiniz ölçütlere uyan dosya satırlarını getirir ve `cut` satırlardan dilimler ve alanlar çıkarır. Farklı dosyalardaki satırları `paste` komutuyla birleştirebilirsiniz.

---

## 🌐 Ağ Yapılandırması ve Veri Aktarımı

`hostname` ve `ip`, ağ yapılandırmasını görüntülemenizi sağlar. `ping` ile bir ağ bağlantısını test edebilir ve `curl` ile `wget` kullanarak veri gönderip alabilirsiniz.

---

## 📦 Sıkıştırmanın Amacı

Sıkıştırma, depolama alanını korur, veri aktarımını hızlandırır ve sistem yükünü azaltır.

---

## 🗜️ Arşivleme ve Sıkıştırma Araçları

`zip`, dosya ve klasörleri arşivlemeden önce sıkıştırır. `tar`, dosya ve dizinleri bir *tarball* içine arşivleyip sıkıştırır. `unzip`, sıkıştırılmış bir zip arşivini açıp sıkıştırmasını çözer ve `tar` da bir `tar.gz` arşivini açıp sıkıştırmasını çözebilir.
