# 📚 Modül 1 Ders 2 Özeti

Tebrikler! Bu dersi tamamladınız. Kursun bu noktasında şunları biliyorsunuz:

* *Structured Query Language* veya  **SQL** , veriyi sorgulamak ve yönetmek için kullanılır.
* SQL, yapılandırılmış verilerle veya varlıklar ve değişkenler arasındaki ilişkileri içeren verilerle çalışmak için kullanışlıdır.
* SQL `SELECT` ifadesi, ilişkisel bir veritabanı tablosundan veri getirir.
* `SELECT` ifadesi bir sorgudur ve bu sorguyu çalıştırdığımızda elde ettiğimiz çıktı bir **Sonuç Kümesi** ( *Result Set* ) veya **Sonuç Tablosu** ( *Result Table* ) olarak adlandırılır.
* En basit biçimiyle bir `SELECT` ifadesinin sözdizimi şudur:

  `SELECT * from TableName`
* SQL `INSERT` ifadesi, satırlar ekleyerek ilişkisel bir veritabanı tablosuna veri ekler.
* `INSERT` ifadesinin sözdizimi şudur:

  `INSERT INTO TableNameColumnName VALUES (values)`
* `INSERT` ifadesi için, *Values* bölümünde sağlanan değerlerin sayısı, *Column Name* listesinde belirtilen sütun adlarının sayısına eşit olmalıdır. Bu, her sütunun bir değeri olmasını sağlar.
* SQL `UPDATE` ifadesi, veriyi okumak ve değiştirmek için kullanılır.
* `UPDATE` ifadesinin sözdizimi aşağıdaki gibidir:

  `UPDATE [TableName] SET [[ColumnName]=[Value]] <WHERE [Condition]>`
* SQL `DELETE` ifadesi, bir tablodan veri silmek için kullanılır.
* `DELETE` ifadesinin sözdizimi şudur:

  `DELETE FROM [TableName] <Where [Condition]>`
* `WHERE` ifadesi ( *WHERE clause* ), `SELECT`, `DELETE` veya `UPDATE` gibi bir SQL ifadesi tarafından üzerinde işlem yapılacak tablo satırlarını belirtir.
