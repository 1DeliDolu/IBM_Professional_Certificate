# 🏁 Özet ve Öne Çıkanlar: Loglama Uygulaması

Tebrikler! Bu dersi tamamladınız. Bu noktada şunları öğrendiniz:

* Yaygın uygulama logları;  **olay (event)** ,  **hata (error)** ,  **erişim (access)** , **performans (performance)** ve **hata ayıklama (debugging)** loglarıdır.
* Çoğu loglama çerçevesi,  **seviyeler** , **log mesajları** ve **hedefler (destinations)** açısından ortak özellikler paylaşır.
* **Yapılandırılmış loglama (structured logging)** , zahmetsiz  **ayrıştırma (parsing)** , **sorgulama (querying)** ve **analitik (analytics)** için standartlaştırılmış formatları mümkün kılar.
* Bir  **log yönetim sistemi (log management system)** , loglardan anlamlı bilgi çıkarmak için dosyaları  **ayrıştırmalıdır (parse)** .
* **Log ayrıştırıcıları (log parsers)** , log yönetim yazılımının motoruna (engine) gömülü olarak bulunur.
* Logları  **izlemek ve analiz etmek** , ağın **gözlemlenebilirliğini (observability)** artırır; **şeffaflık** ve **görünürlük** sağlar.
* Log verisini saklamak;  **sistemlerin güvenilirliği** , ortamların  **güvenlik duruşu (security posture)** , BT sistemlerinde **karar verme süreçlerinin iyileştirilmesi** ve **denetim (auditing)** amaçları dahil olmak üzere farklı nedenlerle gereklidir.
* Günler ya da haftalara yayılan log verisini  **görselleştirmek** , bir uygulamanın davranışı ve performansının zaman içindeki değişimleri hakkında içgörü kazanmak için gereklidir.
* Logları bulutta saklamak, log veri ihtiyaçlarıyla uyumlu **ölçeklenebilir depolama kapasitesi** sağlar.
* Logları saklamak için verileri **kurum içi (on-premises)** sunuculara veya **buluta** yedekleyebilirsiniz.
* Saklama süresinin ne kadar olması gerektiğine dair göreli bir fikir veren bazı analitik boyutlar:  **kritiklik (criticality)** ,  **güvenlik (security)** ,  **olgunluk (maturity)** ,  **sıklık (frequency)** , **maliyet-etkinlik (cost-effectiveness)** ve  **keşif ile çözümleme (discovery and resolution)** .
* Logları saklamaya ilişkin **en iyi uygulamaları (best practices)** dikkate almak önemlidir.
