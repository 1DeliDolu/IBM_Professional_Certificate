# 🚀 DevOps ve GitOps’a Giriş: OpenShift ile

OpenShift Pipelines ile DevOps ve GitOps modülüne hoş geldiniz. Bu modül, sürekli entegrasyon ve sürekli teslimat (CI/CD) dünyasında önemli bir bileşendir.  *OpenShift Pipelines* , CI/CD iş akışlarını oluşturmak, yönetmek ve optimize etmek için tasarlanmıştır.

OpenShift geliştirici ortamına sorunsuz şekilde entegre edilmiş, kullanıcı dostu bir arayüzü öğrenerek başlayacaksınız. Bu arayüzle uygulamanızın DevOps iş akışlarını hassas biçimde tanımlayabilir, orkestre edebilir ve yönlendirebilirsiniz.

Bu modülde, tek bir satır script yazmaya gerek kalmadan somut, gerçek dünya pipeline’ları nasıl oluşturacağınızı öğreneceksiniz.  *OpenShift Pipelines* , siz kullanıcı arayüzü üzerinden görevler ( *tasks* ) ve pipeline’larla çalışırken, zaten aşina olmanız gereken temel teknoloji olan *Tekton* kodunu akıllıca otomatik olarak üretir.

---

## 🧪 Laboratuvar Uygulamaları

Ama eğitim uygulama olmadan neye yarar?

Laboratuvar oturumlarında, önceki modülde Tekton ile sıfırdan yazdığınız bir pipeline’ı yeniden oluştururken yeni becerilerinizi kullanacaksınız. Ancak bu kez kod yazmak yok.

---

## 🌿 GitOps Felsefesine Giriş

OpenShift Pipelines’a derinlemesine dalarken, yalnızca CI/CD’yi keşfetmiyorsunuz; aynı zamanda *GitOps* felsefesine de adım atıyorsunuz. Bu dönüştürücü yaklaşım, uygulama dağıtımlarını yönetmek ve yönetişim sağlamak için sürüm kontrolünün, özellikle  *git* ’in gücünden yararlanır.

---

## 🎛️ Argo CD ile GitOps

Ardından Argo CD geliyor; GitOps aracılığıyla operasyonel mükemmelliğe açılan kapınız.

OpenShift cluster’ınızda çalışan uygulamaları, git depolarınızdaki arzu edilen durum ( *desired state* ) ile sorunsuz biçimde senkronize eden bir araç hayal edin. *Argo CD* ile sadece dağıtımları yönetmiyorsunuz; altyapı, konfigürasyon ve uygulamalardan oluşan bir koleksiyonu sürekli senkron halde tutarak orkestre ediyorsunuz.

---

## 🧩 Kazanımlar

Bu beceri kombinasyonu; CI/CD süreçlerinde ilerlemenizi, dağıtım güvenilirliğini artırmanızı ve DevOps ile GitOps pratikleri üzerinden operasyonları sadeleştirmenizi sağlar.

Haydi, bu yolculuğa birlikte başlayalım.
