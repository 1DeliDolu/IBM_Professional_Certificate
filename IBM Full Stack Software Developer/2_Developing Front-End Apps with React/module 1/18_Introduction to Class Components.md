## 📘 Lesson 2 Summary: Introduction to Class Components

Tebrikler! Bu dersi tamamladınız. Kursun bu noktasında şunları biliyorsunuz:

React bileşenleri, kullanıcı arayüzünü yeniden kullanılabilen ve bağımsız şekilde ele alınabilen ayrı parçalara bölmenizi sağlar.

React bileşenlerinin üç türü  *functional* , *class* ve *high-order* bileşenlerdir.

React’te bir sınıf bileşeni, React kütüphanesindeki React Component sınıfını genişleten bir JavaScript sınıfıdır.

Sınıf bileşenleri state’i yönetir, yaşam döngüsü (lifecycle) olaylarını ele alır ve bileşen metotlarını tanımlar.

React’te props ya da properties, veriyi parent bileşenden child bileşene gönderir.

Event handling, bir React uygulaması içinde tıklamalar, mouseover, mouseout, form gönderimleri ve benzeri kullanıcı etkileşimlerine yanıt verme sürecidir.

State, React’in bileşenin mevcut durumuna dair bilgiyi temsil etmek için kullandığı sade (plain) bir JavaScript nesnesidir.

State verisindeki herhangi bir değişiklik, değişimi yansıtmak için bileşenin yeniden render edilmesine neden olur.

Bileşenler verilerini state’ler ile oluşturup yönetebilirken, dışarıdan gelen veriyi props ile alırlar.

Her React bileşeninin yaşam döngüsü üç temel fazı kapsar:  *mounting* , *updating* ve  *unmounting* .

Bileşen yaşam döngüsünde, bileşenler DOM’a oluşturulur veya mount edilir, güncellenerek büyür ve sonra DOM’dan kaldırılır ya da unmount edilir.
